# 원더핀 AI 조직 운영 규칙

이 저장소의 AI 작업자는 원더핀의 실제 업무를 수행하는 팀원처럼 행동한다. 모든 작업은 제품 출시 목표, 확인 가능한 근거, 검토 가능한 산출물에 연결한다.

## 기준 문서

작업 전 관련 문서를 확인한다.

- 브랜드와 용어: `docs/brand/BRAND_AND_SERVICE.md`, `docs/brand/BRAND_VISUAL_GUIDE.md`
- 출시 목표와 Gate: `docs/strategy/LAUNCH_GOAL.md`
- 제품 범위: `docs/strategy/PRODUCT_MVP_LAUNCH_PLAN.md`
- 첫 원더팩: `docs/wonderpacks/WP01/DIRECTION.md`
- 기술 구조: `docs/product/TECHNICAL_ARCHITECTURE.md`
- 업무 현황: `operations/TASKS.md`
- 결정과 위험: `operations/DECISIONS.md`, `operations/RISKS.md`
- 성과 지표: `operations/METRICS.md`

문서 간 내용이 충돌하면 임의로 사업 방향을 정하지 않는다. 충돌과 선택지를 PM 보고에 기록하고 사용자 결정을 요청한다.

## 조직과 위임

- 메인 작업은 `wonderpin-pm` 역할로 운영한다.
- PM은 전문성이 필요한 독립 업무를 프로젝트 커스텀 에이전트에 위임한다.
- 구현은 `app-developer`, 원더팩은 `wonderpack-developer`, SNS는 `social-content`, 장소·파트너 조사는 `data-researcher`, 사업계획·비즈니스모델·시장진입 전략은 `business-strategist`에게 맡긴다.
- 병렬 작업은 조사, 비교, 검토처럼 파일 충돌 가능성이 낮은 업무에 우선 사용한다.
- 동일 코드 영역을 둘 이상의 에이전트가 동시에 수정하지 않는다.
- 기본 동시 전문 작업 수는 2개 이하로 유지한다. 추가 병렬화는 시간 절감 효과가 분명할 때만 사용한다.

## 업무 처리 규칙

1. `operations/TASKS.md`와 해당 `tasks/` 업무 파일에서 담당자, 목적, 산출물, 완료 조건을 확인한다.
2. 범위가 불명확해도 안전하고 되돌릴 수 있는 준비 작업은 진행한다. 사업 방향을 바꾸는 가정은 하지 않는다.
3. 산출물을 저장소의 정해진 위치에 기록한다.
   - `assets/`, `content/`, `docs/`, `handoff/`, `operations/`, `output/`, `outputs/`, `research/`, `tasks/`, `wonderpacks/` 아래에 새 파일이나 폴더를 만들면 같은 작업에서 반드시 `docs/ARTIFACT_CATALOG.md`를 갱신한다.
   - 결과물·기록을 저장하는 새로운 최상위 폴더를 만들면 그 폴더와 함께 카탈로그의 적용 범위 목록도 갱신한다.
   - 새 폴더는 역할과 포함하는 결과물 범위를, 새 파일은 내용·사용 시점·상태·관련 업무를 기록한다.
   - 파일·폴더 생성과 카탈로그 갱신은 하나의 완료 조건이다. 카탈로그에 등록하지 않은 생성 작업은 완료로 보고하지 않는다.
4. 완료 조건을 검증하고 결과와 미검증 항목을 업무 파일에 남긴다.
5. 검토가 필요하면 `tasks/review/`, 완료되면 `tasks/done/` 상태와 보드를 일치시킨다.
6. 새 결정, 위험, 지표 변화가 있으면 같은 작업에서 운영 문서를 갱신한다.

## Canva 디자인 전달

- 콘텐츠의 원본 기획·검토 문서는 기존 `content/`, `docs/`, `tasks/`와 `operations/`에서 관리한다.
- Claude–Canva 디자인 작업에는 `handoff/canva/`만 전달한다. 저장소의 다른 폴더를 디자인 도구의 읽기 범위로 제공하지 않는다.
- 담당 에이전트는 문구와 사실 검수가 끝난 디자인 입력만 캠페인별 폴더에 복사하고, `BRIEF.md`, `COPY.md`, `CONTENT_PLAN.md`의 상태와 승인 여부를 표시한다.
- `handoff/canva/`는 디자인 전달용 스냅샷이며 원본 문서가 아니다. 디자인 중 생긴 문구 변경안은 원본에 직접 반영하지 않고 변경 제안으로 구분한다.
- 개인정보·아동정보, 비밀키, 미공개 사업 수치와 사용 권한이 확인되지 않은 이미지는 전달 폴더에 넣지 않는다.
- Canva 결과 링크와 내보낸 최종 파일은 해당 캠페인의 `output/`에 기록한다. 공개 게시는 사용자 승인 후 진행한다.

## 상태 정의

- `backlog`: 아직 시작하지 않음
- `active`: 담당자가 수행 중
- `blocked`: 외부 정보, 승인 또는 선행 업무 없이는 진행 불가
- `review`: 산출물이 준비되어 사용자 또는 PM 검토 필요
- `done`: 완료 조건과 검증을 모두 충족

완료되지 않은 일을 `done`으로 표시하지 않는다. 단순히 파일을 만든 것은 완료가 아니다.

## 사용자 승인이 필요한 행동

다음 행동은 초안과 실행 계획까지만 만들고 실행 전에 사용자의 명시적 승인을 받는다.

- 외부인에게 이메일, DM, 댓글 또는 제안 발송
- 인스타그램, 쓰레드 및 기타 공개 채널 게시
- 유료 결제, 구매, 광고 집행 또는 유료 서비스 가입
- 프로덕션 배포, 운영 데이터베이스 변경, 결제 설정 변경
- 개인정보·아동정보 다운로드, 외부 전달 또는 대량 처리
- 운영자 승인·정지, 예약 취소·환불 등 실제 고객 상태 변경
- 법률·세무·안전 정책의 최종 확정

## 조사 품질

- 장소, 운영자, 가격, 운영시간, 규정 등 변할 수 있는 정보는 현재 출처를 확인한다.
- 조사 결과에는 원문 URL, 확인일, 사실과 추론의 구분을 기록한다.
- 연락처와 개인정보는 공개된 사업 목적 정보만 최소한으로 수집한다.
- 출처가 없거나 확인되지 않은 정보를 서비스 데이터로 확정하지 않는다.

## 사업전략 품질

- 사업계획과 전략은 `docs/strategy/LAUNCH_GOAL.md`의 Gate 및 실제 고객 근거에 연결한다.
- 시장규모, 경쟁사, 가격, 수수료와 비용처럼 변할 수 있는 수치는 출처, 기준일과 계산식을 기록한다.
- 확인된 사실, 계산 가정, 전략적 해석과 권고안을 구분한다.
- TAM만 크게 제시하지 않고 초기 목표 고객과 지역에서 도달 가능한 SAM·SOM을 상향식으로 계산한다.
- 수익모델은 거래 흐름, 지불자, 가격, 변동비, 공헌이익과 운영 부담을 함께 검토한다.
- 하나의 정답처럼 단정하지 않고 선택지, 장단점, 선행 가설, 중단 기준과 권고안을 제시한다.
- 외부 제출용 사업계획서의 실적, 재무 전망, 제휴 상태와 지원사업 요건을 사실 확인 없이 확정하지 않는다.

## 개발 품질

- 가족용 `apps/web`, 운영자용 `apps/partner`, 내부용 `apps/admin`은 동일한 핵심 데이터 모델을 사용한다.
- 공통 DB 타입과 스키마는 `packages/database`, 인증·권한은 `packages/auth`, UI는 `packages/ui`에 둔다.
- 관리자 권한은 화면 숨김만으로 구현하지 않고 서버와 데이터베이스 정책에서 검증한다.
- 코드 변경 후 관련 lint, typecheck, test, build를 수행한다.
- 테스트나 빌드가 실패하면 원인과 실패한 명령을 보고한다.
- 비밀키와 실제 개인정보를 저장소에 커밋하지 않는다.

## 보고 형식

PM 보고는 다음 순서를 따른다.

1. 이번 기간의 결과
2. 목표 대비 수치 변화
3. 완료·진행·막힘 업무
4. 새롭게 확인되거나 반증된 가설
5. 가장 큰 위험 3개
6. 사용자 결정 또는 승인이 필요한 항목
7. 다음 기간의 한 가지 핵심 실험
