# WP-006 문서 구조와 Gate 1 KPI 재정비

- 상태: done
- 담당: wonderpin-pm
- 우선순위: P0
- 목표일: 2026-07-14
- 연관 목표: Gate 1 가족 수요·인스타그램 채널 검증

## 목적

인터뷰 중심 Gate 1을 인스타그램과 원더팩의 실제 행동 KPI로 전환하고, `docs/`의 기준 문서·기획·조사 자료를 역할별로 구분한다.

## 산출물

- 인스타그램 행동 KPI가 반영된 출시 목표, 로드맵과 지표판
- 역할별 `docs/` 디렉터리와 문서 지도
- 대체된 장소 조사 초안의 보관 처리
- 변경된 내부 문서 링크

## 완료 조건

- WP-002와 인터뷰 목표가 Gate 1 보드 및 지표에서 제거된다.
- Gate 1 KPI가 발행, 리드, 신청, 실행과 반복 반응으로 정의된다.
- 기준 문서 경로가 `AGENTS.md`와 `docs/README.md`에 반영된다.
- 저장소 내부 Markdown 링크가 유효하다.

## 검증 방법

- 이전 경로와 WP-002 참조를 `rg`로 검색한다.
- Markdown 상대 링크 대상이 실제로 존재하는지 검사한다.

## 결과

- Gate 1을 가족 수요·인스타그램 채널 검증으로 재정의했다.
- 인터뷰 업무 WP-002를 삭제하고 인스타그램 행동 KPI로 교체했다.
- `docs/`를 `strategy`, `brand`, `product`, `wonderpacks`, `growth`, `archive`로 재구성했다.
- `docs/README.md`를 현재 기준 문서의 진입점으로 추가했다.

